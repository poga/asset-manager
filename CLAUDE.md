1. Use TDD
2. Always use `uv run` instead of `python` to run scripts
3. During development, assume API (port 8000) and frontend (port 5173) servers are already running. Do not start them - just use them.
